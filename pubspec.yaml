name: simply_responsive
description: Utility UI library to help us build a responsive app with ease.
version: 0.0.2
repository: https://github.com/moseskarunia/simply-responsive

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
