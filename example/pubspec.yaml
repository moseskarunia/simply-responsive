name: example
description: A new Flutter project.

publish_to: "none"

version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  simply_responsive:
    path: ../
  lorem_ipsum: any
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
